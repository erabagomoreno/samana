# assert 'a4_a' not in list(kwargs_prior.keys())
# assert 'a3_a' not in list(kwargs_prior.keys())
# assert 'delta_phi_m3' not in list(kwargs_prior.keys())
# assert 'delta_phi_m4' not in list(kwargs_prior.keys())
# a4a_population_mean_prior_type = kwargs_prior['a4a_pop'][0]
# a3a_population_mean_prior_type = kwargs_prior['a3a_pop'][0]
# a4a = np.random.normal(
#     kwargs_prior['a4a_pop'][1],
#     kwargs_prior['a4a_pop'][2]
# )
# a3a = np.random.normal(
#     kwargs_prior['a3a_pop'][1],
#     kwargs_prior['a3a_pop'][2]
# )
#
# prior_samples_dict['a3_a_pop'] = kwargs_prior['a3a_pop'][1]
# prior_samples_dict['a3_a_pop'] = kwargs_prior['a3a_pop'][2]
# prior_samples_dict['a4_a_pop'] = kwargs_prior['a4a_pop'][1]
# prior_samples_dict['a4_a_pop'] = kwargs_prior['a4a_pop'][2]
# prior_samples_dict['delta_phi_m3_pop'] = kwargs_prior['delta_phi_m3_pop'][1]
# prior_samples_dict['delta_phi_m3_pop'] = kwargs_prior['a4a_pop'][2]
# prior_samples_dict['delta_phi_m3_pop'] = delta_phi_m3
# prior_samples_dict['delta_phi_m4_pop'] = delta_phi_m4
#
# prior_samples_dict['a3_a'] = a3a
# prior_samples_dict['a4_a'] = a4a
# prior_samples_dict['delta_phi_m3'] = delta_phi_m3
# prior_samples_dict['delta_phi_m4'] = delta_phi_m4
# sample_list += [a3a, a4a, delta_phi_m3, delta_phi_m4]
# sample_names += ['a3_a', 'a4_a', 'delta_phi_m3', 'delta_phi_m4']
# joint_multipole_prior_used = True
